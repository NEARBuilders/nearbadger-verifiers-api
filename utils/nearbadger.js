const NearBadger = {
  issue: (accountId, handle, proof) => {
    // Instantiate our wallet
    // Get the message to sign (accountId + handle + proof + nonce)
    // Sign the message
    // Return signed message
  }
}

module.exports = NearBadger;
